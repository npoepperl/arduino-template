# CMakeLists.txt Roti

SET(MODULE_NAME Roti)

file(GLOB_RECURSE MODULE_SOURCECES
    "*.h"
    "*.cpp")

ADD_LIBRARY(${MODULE_NAME} STATIC
    ${MODULE_SOURCECES})

TARGET_INCLUDE_DIRECTORIES(${MODULE_NAME} PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/include)